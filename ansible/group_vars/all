---
# Set this to true to create a john@[your domain] account with password 'summersun'
ispmail_populate_test_data: true

### If you do not set the domain below then this playbook will
### determine your public IP address and create a certificate for
### ip.ad.dre.ess.nip.io. So by default you will get a working setup.
ispmail_fqdn: nnxx.ninux.org

# MySQL password for read-only user
ispmail_mysql_mailserver_password: 'Change;me'

# MySQL password for read-write user
ispmail_mysql_mailadmin_password: 'Change;me'

# MySQL password for administrative root user
ispmail_mysql_root_password: 'Change;me'

# MySQL password for storing Roundcube configuration
ispmail_mysql_roundcube_password: 'Change;me'

# MySQL password for storing Horde webmail user configuration
ispmail_mysql_horde_password: 'Change;me'

# MySQL password for phpMyAdmin - user pma
ispmail_mysql_pma_password: "Change;me"

# Allowed authentication mechanisms for IMAP/POP3
# See: https://wiki2.dovecot.org/Authentication/Mechanisms
ispmail_dovecot_auth_mechanisms: plain login

# The email address used for administrative purposes
ispmail_postmaster_address: postmaster@"{{ ispmail_fqdn }}"

# Password for rspamd web interface authentication at
# https://…/rspamd
ispmail_rspamd_web_password: 'Change;me'

certbot_create_if_missing: false
certbot_create_standalone_stop_services:
  - nginx
  - dovecot
  - postfix

# Enable or no Let's Encrypt
lets: false

# Self-signed certificate
ispmail_certificate_country: "IT"
ispmail_certificate_state: "Italy"
ispmail_certificate_location: "Matera"
ispmail_certificate_organisation: "Ninux Basilicata"
ispmail_certificate_orgunit: "Ninux"
ispmail_certificate_domains: "{{ ispmail_fqdn }}"
ispmail_certificate_email: "{{ ispmail_postmaster_address }}"
ispmail_certificate_days_valid: "9999"

# DKIM
dkim_admin_email: "{{ ispmail_postmaster_address }}"
dkim_selector: mail
dkim_domains:
  - "{{ ispmail_fqdn }}"
dkim_same_key: false

# Roundcube
roundcube_htdocs: "/var/www/sites/mail.{{ ispmail_fqdn }}"
roundcube_db_password: "Change;me"
roundcube_mail_server: "mail.{{ ispmail_fqdn }}"

# ISPMailAdmin
ispmailadmin_htdocs: "/var/www/sites/ispmail.{{ ispmail_fqdn }}"
ispmailadmin_db_pass_root: "Change;me"
ispmailadmin_db_user: mailadmin
ispmailadmin_db_pass: "Change;me"
ispmailadmin_db_name: mailserver
ispmailadmin_db_host: localhost
ispmailadmin_user: ninux
ispmailadmin_pass: NinuxBas2022
ispmailadmin_access: account
ispamailadmin_pass_hashes: sha256
ispmailadmin_version: 0.9.9

# Nginx
apps:
  - "ispmail.{{ ispmail_fqdn }}"
  - "mail.{{ ispmail_fqdn }}"

#phpMyAdmin
phpmyadmin_version: 5.2.1
controlpass: 'Change;me'

